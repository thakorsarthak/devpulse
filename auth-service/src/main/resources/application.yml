spring:
  application:
    name: auth-service
  config:
    import: "optional:configserver:http://localhost:8888"
#Everything else — datasource, JPA, JWT, Flyway — comes from Config Server. This is the point of Config Server.